Angular-Surveys-Management-System -> download NPM and use ng serve command to run. Runs on localhost:4000 by default.

API -> download XAMPP and place this folder in the htdocs of XAMPP. Run XAMPP control and start Apache and SQL.

Memberdb -> go to localhost/phpmyadmin and enter these queries to create the database and tables.